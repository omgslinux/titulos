{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tipos de enlace</h1>

    <table>
        <tbody>
            <tr>
                <th>Ley</th>
                <th>Descripción corta</th>
            </tr>
            <tr>
                <td><a href="{{ path('manage_laws_edit', { 'id': laws.id }) }}">{{ laws.shortname }}</a></td>
                <td>{{ laws.shortname}}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('manage_laws_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
