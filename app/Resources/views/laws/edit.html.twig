{% extends 'custom.html.twig' %}

{% block body %}
    <h1>Edición de leyes</h1>

{{ block('crudform') }}
    <ul>
        <li>
            <a href="{{ path('manage_laws_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
