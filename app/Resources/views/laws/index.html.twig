{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lista de leyes</h1>

    <table>
        <thead>
            <tr>
                <th>Número</th>
                <th>Fecha</th>
                <th>Descripción</th>
            </tr>
        </thead>
        <tbody>
        {% for law in laws %}
            <tr>
                <td>{{ law.number }}</td>
                <td>{{ law.lawdate|date('d/m/Y') }}</td>
                <td><a href="{{ path('manage_laws_show', { 'id': law.id }) }}">{{ law.shortname }}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('manage_laws_new') }}">Crear una ley</a>
        </li>
    </ul>
    <p><a href="{{ path('manage') }}">Volver al menú de gestión</a></p>
{% endblock %}
