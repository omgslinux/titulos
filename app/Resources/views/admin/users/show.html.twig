{% extends 'base.html.twig' %}

{% block body %}
    <h1>Usuarios</h1>

    <table>
        <tbody>
            <tr>
                <th>Login</th>
                <th>Nombre completo</th>
            </tr>
            <tr>
                <td><a href="{{ path('manage_users_edit', { 'id': user.id }) }}">{{ user.username }}</a></td>
                <td>{{ user.fullname }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
